apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: forager-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "gear-ingress-ext-ip"
spec:
  defaultBackend:
    service:
      name: forager-ingress-service
      port:
        number: 8080