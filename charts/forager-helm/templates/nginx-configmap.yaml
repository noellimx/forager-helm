apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  nginx.conf: |
    # nginx.conf

    events {}

    http {
        server {
            location / {
                # Using the `echo` module to return the value of the environment variable
                echo "Received variable: $my_variable";
            }
        }
    }
